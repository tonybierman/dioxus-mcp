# Demo fixture for dioxus-mcp integration tests.
# Never built by cargo; only its source is parsed by syn.
[package]
name = "sample-project"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
dioxus = { version = "0.7", features = ["fullstack", "web", "server"] }
